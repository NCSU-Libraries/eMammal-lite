<% flash.each do |message_type, message| %>
  <div class="alert alert-<%= message_type %> visible">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <ul>
        <h1 class="lg-text"><%= message %></h1>
    </ul>
  </div>
<% end %>
